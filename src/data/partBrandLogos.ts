/**
 * Logo files for the parts brands shown inside each category.
 *
 * To add a logo: put the file in public/images/part-brands/ and add a line
 * here, e.g.  'ZF': '/images/part-brands/zf.svg',
 * The key must match the brand name in src/data/brandProfiles.ts.
 *
 * Brands without a logo here are shown as a clean typed name instead.
 * SVG or a transparent PNG works best; logos sit on a white tile.
 */

export const PART_BRAND_LOGOS: Record<string, string> = {};
